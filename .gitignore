# dependencies (re-installed with `npm install`)
node_modules/

# build output (generated by `npm run build`)
dist/

# logs / editor / OS noise
*.log
.DS_Store
.vscode/
.idea/

# never let real brain data leak into the public app repo
inbox.md
graph.json
/para/
/entities/
